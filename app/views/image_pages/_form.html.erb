<%= form_for(@image_page) do |f| %>
  <div class="field">
    <%= f.hidden_field :webpage_id %>
  </div>
  <div class="field">
    <%= f.hidden_field :image_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
