<%= form_for(@webpage) do |f| %>
  <% if @webpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webpage.errors.count, "error") %> prohibited this webpage from being saved:</h2>

      <ul>
      <% @webpage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.select :type, Webpage::PAGES.map {|x| [x]}%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
