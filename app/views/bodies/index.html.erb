<p id="notice"><%= notice %></p>

<h1>Listing Bodies</h1>

<table>
  <thead>
    <tr>
      <th>Subheader</th>
      <th>Paragraph</th>
      <th>Webpage</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bodies.each do |body| %>
      <tr>
        <td><%= body.subheader %></td>
        <td><%= body.paragraph %></td>
        <td><%= body.webpage_id %></td>
        <td><%= link_to 'Show', body %></td>
        <td><%= link_to 'Edit', edit_body_path(body) %></td>
        <td><%= link_to 'Destroy', body, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Body', new_body_path %>
